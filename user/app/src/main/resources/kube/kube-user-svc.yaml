---
apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: example
  labels:
  	name: example
  	app: example
spec:
  selector:
    name: example
    app: example
  type: NodePort
  ports:
  - name: api
    port: 8080
