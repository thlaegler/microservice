server:
  port: ${ORG_EXAMPLE_GATEWAY_PORT:8080}
security:
  ignored: /**
  basic:
    enabled: false
  enable-csrf: false
spring:
  aop:
    auto: false
  security:
    enabled: false
management:
  security:
    enabled: false